# Руководство по проверке работоспособности бэкенда

## Содержание
1. [Предварительная подготовка](#предварительная-подготовка)
2. [Локальная проверка](#локальная-проверка)
3. [Проверка через Docker](#проверка-через-docker)
4. [Ручное тестирование API](#ручное-тестирование-api)
5. [Автоматическое тестирование](#автоматическое-тестирование)
6. [Проверка логов](#проверка-логов)
7. [Диагностика проблем](#диагностика-проблем)

---

## Предварительная подготовка

### 1. Проверка зависимостей

Убедитесь, что установлены все необходимые компоненты:

```bash
# Проверка версии Python (должна быть 3.10+)
python --version

# Проверка установки PostgreSQL (если используется локально)
psql --version

# Проверка Docker (если используется Docker)
docker --version
docker-compose --version
```

### 2. Установка зависимостей Python

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

#### Генерация SECRET_KEY

**SECRET_KEY** - это критически важный ключ для безопасности Django. Он используется для:
- Шифрования сессий
- Подписи cookies
- Генерации токенов CSRF
- И других криптографических операций

**Никогда не используйте один и тот же SECRET_KEY в разных проектах!**

##### Способ 1: Через Python (рекомендуется)

```bash
# Активация виртуального окружения (если еще не активировано)
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Генерация ключа
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Пример вывода:**
```
django-insecure-abc123xyz789... (длинная случайная строка)
```

##### Способ 2: Через Django shell

```bash
python manage.py shell
```

Затем в Python shell:
```python
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```

##### Способ 3: Онлайн генератор

Используйте официальный генератор Django:
- https://djecrety.ir/ (Django Secret Key Generator)
- Или любой другой надежный генератор случайных строк

##### Способ 4: Через утилиту (автоматически)

Создайте файл `generate_secret_key.py` в корне проекта:

```python
from django.core.management.utils import get_random_secret_key

secret_key = get_random_secret_key()
print(f"SECRET_KEY={secret_key}")
```

Запустите:
```bash
python generate_secret_key.py
```

#### Создание файла .env

Создайте файл `.env` в корне проекта и вставьте сгенерированный ключ:

```env
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DB_HOST=localhost
DB_NAME=blog_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
```

**Важно:** В production измените `SECRET_KEY` на безопасный случайный ключ!

---

## Локальная проверка

### Шаг 1: Запуск PostgreSQL

#### Вариант A: Через Docker Compose (рекомендуется)

```bash
# Запуск только базы данных
docker-compose up -d db

# Проверка статуса
docker-compose ps
```

#### Вариант B: Локальный PostgreSQL

Убедитесь, что PostgreSQL запущен и доступен на порту 5432.

### Шаг 2: Применение миграций

```bash
# Создание миграций (если еще не созданы)
python manage.py makemigrations

# Применение миграций
python manage.py migrate
```

**Ожидаемый результат:** Миграции применены успешно, таблицы созданы в базе данных.

### Шаг 3: Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введите:
- Username: `admin` (или любой другой)
- Email: (можно оставить пустым)
- Password: (введите надежный пароль)

### Шаг 4: Запуск сервера разработки

```bash
python manage.py runserver
```

**Ожидаемый результат:** 
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

### Шаг 5: Проверка доступности

Откройте в браузере:
- **Корневой endpoint:** http://127.0.0.1:8000/ (возвращает информацию об API)
- **API документация:** http://127.0.0.1:8000/api/docs
- **Админ-панель:** http://127.0.0.1:8000/admin/

Если страницы открываются, сервер работает корректно!

---

## Проверка через Docker

### Шаг 1: Запуск всех сервисов

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f web
```

### Шаг 2: Применение миграций

```bash
docker-compose exec web python manage.py migrate
```

### Шаг 3: Создание суперпользователя

```bash
docker-compose exec web python manage.py createsuperuser
```

### Шаг 4: Проверка работоспособности

Откройте в браузере:
- http://localhost:8000/api/docs
- http://localhost:8000/admin/

---

## Ручное тестирование API

### Инструменты для тестирования

Рекомендуемые инструменты:
- **Postman** - https://www.postman.com/
- **curl** - командная строка
- **Swagger UI** - встроенная документация по адресу `/api/docs`

### 1. Регистрация пользователя

```bash
curl -X POST http://localhost:8000/api/blog/register/ \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "testpass123"}'
```

**Ожидаемый результат:**
```json
{
  "message": "Пользователь успешно зарегистрирован",
  "user_id": 1
}
```

### 2. Вход (получение JWT токена)

```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "testpass123"}'
```

**Ожидаемый результат:**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

**Сохраните токен `access` для следующих запросов!**

### 3. Создание категории

```bash
curl -X POST http://localhost:8000/api/blog/categories/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{"name": "Технологии", "description": "Статьи о технологиях"}'
```

### 4. Создание статьи

```bash
curl -X POST http://localhost:8000/api/blog/articles/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "title": "Моя первая статья",
    "content": "Содержание статьи",
    "published": true,
    "category_id": 1
  }'
```

### 5. Получение списка статей

```bash
curl http://localhost:8000/api/blog/articles/
```

### 6. Получение статьи по ID

```bash
curl http://localhost:8000/api/blog/articles/1/
```

### 7. Обновление статьи

```bash
curl -X PUT http://localhost:8000/api/blog/articles/1/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "title": "Обновленный заголовок",
    "content": "Обновленное содержание"
  }'
```

### 8. Создание комментария

```bash
curl -X POST http://localhost:8000/api/blog/comments/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "article_id": 1,
    "content": "Отличная статья!"
  }'
```

### 9. Получение комментариев к статье

```bash
curl http://localhost:8000/api/blog/articles/1/comments/
```

### 10. Удаление статьи

```bash
curl -X DELETE http://localhost:8000/api/blog/articles/1/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

### Проверка ошибок

#### Тест 1: Регистрация с существующим username

```bash
curl -X POST http://localhost:8000/api/blog/register/ \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "testpass123"}'
```

**Ожидаемый результат:** Статус 400, сообщение об ошибке

#### Тест 2: Создание статьи без авторизации

```bash
curl -X POST http://localhost:8000/api/blog/articles/ \
  -H "Content-Type: application/json" \
  -d '{"title": "Статья", "content": "Содержание"}'
```

**Ожидаемый результат:** Статус 401 (Unauthorized)

#### Тест 3: Обновление чужой статьи

```bash
# Создайте второго пользователя и попробуйте обновить статью первого
curl -X PUT http://localhost:8000/api/blog/articles/1/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_DIFFERENT_USER" \
  -d '{"title": "Взлом"}'
```

**Ожидаемый результат:** Статус 403 (Forbidden)

---

## Автоматическое тестирование

### Запуск всех тестов

```bash
# Запуск всех тестов
pytest

# С подробным выводом
pytest -v

# С покрытием кода
pytest --cov=blog --cov-report=html

# Запуск конкретного теста
pytest blog/tests.py::ArticleAPITestCase::test_create_article_success
```

### Ожидаемый результат

Все тесты должны пройти успешно:
```
========================= test session starts =========================
collected 20 items

blog/tests.py::ArticleAPITestCase::test_list_articles_success PASSED
blog/tests.py::ArticleAPITestCase::test_create_article_success PASSED
...
========================= 20 passed in 2.34s =========================
```

### Структура тестов

Тесты покрывают:
- ✅ Регистрацию пользователей
- ✅ JWT аутентификацию
- ✅ CRUD операции для статей
- ✅ CRUD операции для комментариев
- ✅ CRUD операции для категорий
- ✅ Проверку прав доступа (автор может редактировать только свои статьи/комментарии)
- ✅ Обработку ошибок (404, 401, 403)

---

## Проверка логов

### Просмотр логов в реальном времени

```bash
# Локально
tail -f blog.log

# В Docker
docker-compose logs -f web
```

### Что проверять в логах

1. **Успешные операции:**
   - `user_registered` - регистрация пользователя
   - `user_logged_in` - вход пользователя
   - `article_created` - создание статьи
   - `crud_operation` - CRUD операции

2. **Ошибки:**
   - `register_failed` - ошибка регистрации
   - `user_login_failed` - неудачный вход
   - `article_update_denied` - попытка редактировать чужую статью

### Примеры логов

**Успешная регистрация:**
```
INFO user_registered user_id=1 username=testuser
```

**Создание статьи:**
```
INFO crud_operation operation=create model=Article user_id=1 object_id=1
INFO article_created article_id=1 author_id=1
```

**Ошибка доступа:**
```
WARNING article_update_denied article_id=1 user_id=2
```

---

## Диагностика проблем

### Проблема 1: Ошибка подключения к базе данных

**Симптомы:**
```
django.db.utils.OperationalError: connection to server at "localhost" (::1), port 5432 failed
```

**Решение:**
1. Проверьте, запущен ли PostgreSQL:
   ```bash
   # Windows
   services.msc
   
   # Linux
   sudo systemctl status postgresql
   ```

2. Проверьте настройки в `.env`:
   - `DB_HOST` - должен быть `localhost` или `db` (для Docker)
   - `DB_PORT` - должен быть `5432`
   - `DB_NAME`, `DB_USER`, `DB_PASSWORD` - должны совпадать с настройками PostgreSQL

3. Для Docker:
   ```bash
   docker-compose up -d db
   docker-compose ps  # Проверьте статус
   ```

### Проблема 2: Ошибка миграций

**Симптомы:**
```
django.db.migrations.exceptions.InconsistentMigrationHistory
```

**Решение:**
```bash
# Удалите все миграции (кроме __init__.py)
# Затем создайте заново:
python manage.py makemigrations
python manage.py migrate
```

### Проблема 3: Ошибка импорта модулей

**Симптомы:**
```
ModuleNotFoundError: No module named 'ninja'
```

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
# Переустановите зависимости:
pip install -r requirements.txt
```

### Проблема 4: Ошибка 401 (Unauthorized)

**Симптомы:**
При запросе с токеном получаете 401

**Решение:**
1. Проверьте формат заголовка:
   ```
   Authorization: Bearer YOUR_TOKEN
   ```
   (обратите внимание на пробел после "Bearer")

2. Проверьте, не истек ли токен (срок жизни - 1 день)

3. Получите новый токен через `/api/auth/login/`

### Проблема 5: Ошибка 500 (Internal Server Error)

**Симптомы:**
Сервер возвращает 500 при любом запросе

**Решение:**
1. Проверьте логи:
   ```bash
   tail -f blog.log
   ```

2. Проверьте настройки в `settings.py`

3. Убедитесь, что все миграции применены:
   ```bash
   python manage.py migrate
   ```

### Проблема 6: Порт 8000 уже занят

**Симптомы:**
```
Error: That port is already in use.
```

**Решение:**
```bash
# Используйте другой порт:
python manage.py runserver 8001

# Или найдите и завершите процесс на порту 8000
# Windows:
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux:
lsof -ti:8000 | xargs kill
```

---

## Чек-лист проверки работоспособности

Используйте этот чек-лист для быстрой проверки:

- [ ] PostgreSQL запущен и доступен
- [ ] Виртуальное окружение активировано
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и настроен
- [ ] Миграции применены (`python manage.py migrate`)
- [ ] Суперпользователь создан (`python manage.py createsuperuser`)
- [ ] Сервер запускается без ошибок (`python manage.py runserver`)
- [ ] API документация доступна (`http://localhost:8000/api/docs`)
- [ ] Админ-панель доступна (`http://localhost:8000/admin/`)
- [ ] Регистрация пользователя работает
- [ ] JWT аутентификация работает
- [ ] Создание статьи работает
- [ ] Получение списка статей работает
- [ ] Обновление статьи работает
- [ ] Удаление статьи работает
- [ ] Создание комментария работает
- [ ] Все тесты проходят (`pytest`)
- [ ] Логи записываются корректно

---

## Дополнительные проверки

### Проверка производительности

```bash
# Установите Apache Bench (ab)
# Windows: скачайте с Apache website
# Linux: sudo apt-get install apache2-utils

# Тест нагрузки (100 запросов, 10 одновременно)
ab -n 100 -c 10 http://localhost:8000/api/blog/articles/
```

### Проверка безопасности

1. **Проверьте, что DEBUG=False в production**
2. **Используйте сильный SECRET_KEY**
3. **Настройте ALLOWED_HOSTS для production**
4. **Используйте HTTPS в production**

### Мониторинг

Для production рекомендуется настроить:
- Мониторинг ошибок (Sentry, Rollbar)
- Мониторинг производительности (New Relic, Datadog)
- Логирование в централизованную систему (ELK Stack)

---

## Заключение

Если все проверки пройдены успешно, ваш бэкенд готов к работе! 

При возникновении проблем:
1. Проверьте логи (`blog.log`)
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки базы данных
4. Запустите тесты для диагностики

**Удачной разработки!** 🚀

